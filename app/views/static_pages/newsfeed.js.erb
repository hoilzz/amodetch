<% unless params[:search].nil?%>
    <% unless params[:major].nil?%>
		$('.major-btn').html('<%= params[:major]%>');
		$('#searchtables').html('<%= escape_javascript(render("searchtables")) %>')
		$('.pagination a').attr('data-remote', 'true');
 	<%else%>
	$('#searchtables').html('<%= escape_javascript(render("searchtables")) %>');
	$('.pagination a').attr('data-remote', 'true');
	<%end%> 

<% else %>
		$('.major-btn').html('<%= params[:major]%>');
		$('#searchtables').html('<%= escape_javascript(render("searchtables")) %>')
		$('.pagination a').attr('data-remote', 'true');
	
<% end %> 
