<div class = "container">
  <div class = "responsive-img main_blk">
  </div>
</div>

<div class = "row title_blk">
  <h3 class="col s12 light center header">아뭐듣지</h3>
  <p class="light center">세종대 시간표를 가장 간단하게 작성할 수 있습니다-</p>
</div>
  <div class = "container info_blk">
    <div class = "row">
      <div class = "col m8 offset-m2">
    <div class="row">
      <div class="col m4">
        <div class="center promo">
          <p class="promo-caption"><%= Valuation.all.count %></p>
          <p class="promo-caption">강의평가</p>
          <!-- <p class="light center">저희는 여러분들에게 사용자 지정 구성요소를 포함하는 기본적인 스타일을 제공해줍니다. 또한, 저희는 애니메이션과 움직이는 요소들을 다듬어 개발자들에게 좀 더 원활한 경험을 재공해줍니다.</p> -->
        </div>
      </div>
      <div class="col m4">
        <div class="center promo">
          <p class="promo-caption"><%= Timetable.all.count %></p>
          <p class="promo-caption">시간표</p>
          <!-- <p class="light center">저희는 여러분들에게 사용자 지정 구성요소를 포함하는 기본적인 스타일을 제공해줍니다. 또한, 저희는 애니메이션과 움직이는 요소들을 다듬어 개발자들에게 좀 더 원활한 경험을 재공해줍니다.</p> -->
        </div>
      </div>
      <div class="col m4">
        <div class="center promo">
          <p class="promo-caption"><%=User.all.count%></p>
          <p class="promo-caption">학생</p>
          <!-- <p class="light center">저희는 여러분들에게 사용자 지정 구성요소를 포함하는 기본적인 스타일을 제공해줍니다. 또한, 저희는 애니메이션과 움직이는 요소들을 다듬어 개발자들에게 좀 더 원활한 경험을 재공해줍니다.</p> -->
        </div>
      </div>
    </div>
</div>
  </div>
  </div>


<script>
  <% if !@lecture_name.nil? %>
    $('.dropdown-toggle').dropdown().html('<%=@lecture_name%>');
  <% end %>

  $(function(){
    $(".dropdown-menu").on('click', 'li a', function(){
      $("#border-none:first-child").text($(this).text());
      $("#border-none:first-child").val($(this).text());
  });

});
function showLecture(lec_num)
  {
      <% if user_login? %>
        <% if current_user.valuations.count>=2 %>
          window.location="/lectures/"+lec_num.toString()
         <% else %>
         window.location="<%=forcingwritting_url%>"
         <% end %>
       <% else %>
          window.location="<%=root_url%>"
      <% end %>
  }
</script>
