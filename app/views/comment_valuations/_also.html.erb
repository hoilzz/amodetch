<%= form_for comment.comment_valuations.build, remote: true do |f| %>
			<%= hidden_field_tag :lecture_id, comment.lecture_id %>
			<%= hidden_field_tag :comment_id, comment.id %>
			<%= f.submit "옳소", class: "btn btn-link also_unalso_btn"%>
		<% end %>